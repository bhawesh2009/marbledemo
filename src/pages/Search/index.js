import React from 'react';
import ProductResults from './../../components/ProductResults';

const Search = ({ }) => {// eslint-disable-next-line 
  return (
    <div className="searchPage">
      <ProductResults />
    </div>
  );
};

export default Search;
